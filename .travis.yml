os:
  - linux
language: php
dist: bionic
php:
  - '8.0'
install: skip
script: skip
services:
  - docker
  - nginx
  - postgresql
jobs:
  include:
    - stage: "Tests"                
      name: "Unit Test PHP"
      script: 
      - travis_retry composer self-update
      - travis_retry composer install --prefer-source --no-interaction
      - cp .env.example .env
      - php artisan key:generate
      - vendor/bin/phpunit tests/Unit/ExampleTest.php
    - stage: "Build Docker Image"
      name: "Build and Push Images Docker" 
      script:
      - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin
      - docker build -t hk .
      - docker images
      - docker tag hk $DOCKER_USERNAME/hk-php-nginx
      - docker push $DOCKER_USERNAME/hk-php-nginx
notifications:
  email: agarizki46@gmail.com